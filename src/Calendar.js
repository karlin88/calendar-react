import React, { Component } from 'react';
import Header from './Header';

class Calendar extends Component {
    week1() {
        this.props.calendar.firstmonthday
    }

    render() {
        const { calendar } = this.props;

        return(


        );
    }
}

export default Calendar;